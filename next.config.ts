import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    remotePatterns: [new URL('https://www.pexels.com/photo/close-up-photo-of-programming-of-codes-546819/')],

  }
};

export default nextConfig;
